{% schema %}
{
  "name": "About Story",
  "settings": [
    {
      "type": "text",
      "id": "small_heading",
      "label": "Small Heading",
      "default": "SINCE 2012"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Main Heading",
      "default": "Family Business, Mountain Roots"
    },
    {
      "type": "richtext",
      "id": "content",
      "label": "Content",
      "default": "<p>Founded in 2012 in Truckee, California 89 has grown from a small local shop into a family-owned business that celebrates the Sierra Nevada lifestyle. Our mission is to bring the beauty and inspiration of mountain living to homes and wardrobes everywhere.</p><p>Every product we curate tells a story of adventure, community, and the simple pleasures of life on Highway 89. From cozy apparel to handpicked home decor, we're committed to quality, authenticity, and the values that make mountain living special.</p>"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Image"
    }
  ],
  "presets": [
    {
      "name": "About Story"
    }
  ]
}
{% endschema %}

<div class="about-story">
  <div class="container">
    <div class="about-story-grid">
      <div class="about-story-content">
        {%- if section.settings.small_heading != blank -%}
          <p class="about-story-small-heading">{{ section.settings.small_heading }}</p>
        {%- endif -%}
        
        {%- if section.settings.heading != blank -%}
          <h2 class="about-story-heading">{{ section.settings.heading }}</h2>
        {%- endif -%}
        
        {%- if section.settings.content != blank -%}
          <div class="about-story-text">
            {{ section.settings.content }}
          </div>
        {%- endif -%}
      </div>
      
      <div class="about-story-image">
        {%- if section.settings.image != blank -%}
          <img 
            src="{{ section.settings.image | image_url: width: 800 }}"
            alt="{{ section.settings.heading | escape }}"
            loading="lazy"
          >
        {%- endif -%}
      </div>
    </div>
  </div>
</div>
